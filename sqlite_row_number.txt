-- SELECT  WORK_SEQ, START_DATETIME, substr(START_DATETIME,15, 5)AS MIN_SEC , RST_TYPE
-- FROM SMPS_WORK_RST 
-- WHERE  WORK_SEQ = "WK0000067" 
-- 
-- ORDER BY START_DATETIME ASC

SELECT 
	substr(START_DATETIME,15, 5)AS MIN_SEC , RST_TYPE,
	row_number() OVER(
		PARTITION BY substr(START_DATETIME,15, 5)
		ORDER BY START_DATETIME
	) RowNum
	
FROM SMPS_WORK_RST
WHERE WORK_SEQ = "WK0000067" 